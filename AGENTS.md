# PaiResume 协作约定

## 当前产品基线

- 首页已确认基本完成，当前阶段默认冻结。除非用户明确要求，不再主动调整首页布局、文案和动效。
- 首页如需继续修改，只使用真实业务数据；不得虚构评分、浏览量、成交量或用户评价，优先展示免费/付费、价格等已有字段。
- 当前重点是上线安全与业务闭环，不是继续打磨展示层。上线范围按“免费或邀请灰度 → 会员支付 → 用户付费简历市场”逐步开放。
- 下述上线阻断项完成前，不正式开放收款；用户发布的付费简历市场保持关闭，首发只允许官方精选内容。
- 对外文案和入口必须读取服务端业务开关；市场关闭时不得宣传成当前可购买、可投稿。

## 用户界面文案规则

- 默认不添加常驻的解释性副标题、帮助文案或实现细节。控件名称、布局和状态已经能表达含义时，不再用文字重复解释。
- 禁止使用“已按……推算，可修改”“只影响……”“不会覆盖……”“点击后……”等描述显而易见行为的文案。若必须依靠说明才能理解，应先改进名称、结构、交互或状态反馈。
- 仅保留会影响用户决策或操作结果的信息：输入与上传限制、可执行的错误恢复、保存和处理状态、付费与退款结果、不可逆操作后果，以及隐私、安全、授权和法律提示。
- 新增辅助文案前必须回答“删掉后是否会导致用户做错、无法恢复或承担未知风险”；答案为否时不添加。修改同类文案时应全局搜索相同模式，不能只处理用户指出的单个位置。

## 用户界面容器规则

- 同一块内容默认只保留一层承载容器，不为制造层次连续叠加圆角、边框、背景和阴影；禁止无语义的“卡片套卡片”。
- 优先用留白、字号字重、浅色背景和单向分隔线表达层级。输入控件、弹窗、错误提示、可独立操作的重复条目和拖拽项可以保留明确边界。
- 发现一处冗余套框时，必须同时检查对应的复用组件和同类用户页面，不能只删除截图中指出的那一层。

## 当前实施状态

- 三阶段业务代码、搜索引擎优化、账户与隐私闭环、自动保存、安全边界和发布运维脚本均已落地；旧支付总开关、会员新订单和用户市场默认关闭。
- 登录目标主入口已改为“派聪明”服务号扫码注册/登录：PaiResume 不保存 AppSecret/access_token，只通过 paicoding 双向 HMAC 网关生成临时二维码并接收可信事件；纯扫码账号邮箱和密码为空，注销前必须重新扫码取得一次性凭证。2026-07-24 首发为避免修改或重启现有 paicoding 服务，扫码桥暂时关闭，邮箱兼容登录作为当前可用入口。
- “派聪明”只负责登录。人工精修固定为：第一次免费；第二次及以后每份单独付费。产品不再提供“关注沉默王二后第二次免费”的权益，不展示第二个二维码，也不再接入独立关注桥。
- 普通 PDF 导出只在浏览器客户端完成，服务端不再渲染或中转导出文件。人工精修改为用户选择本地 PDF，浏览器使用服务端签发的短期、单对象上传地址直传私有 OSS；服务端校验后在 OSS 内固化不可覆盖对象，再从 OSS 临时读取并投递到固定私密收件箱，不写服务器本地磁盘。SMTP 受理后才核销免费次数，已发送副本不可召回。人工精修总开关 `RESUME_REVIEW_ENABLED` 与真实单价默认均关闭/为 `0`；总开关关闭时不得发送精修验证码、签发/完成上传、创建新请求或投递精修邮件，但历史查单、退款、支付回调和对账继续工作。
- 付费 VIP 提供月卡 `30` 天、季卡 `90` 天、年卡 `365` 天和终身会员，方案编码、权益类型、期限与价格由服务端 `membership_plan` 管理并固化进订单快照；迁移时只继承并启用已有年卡价格，其余方案必须由管理员配置真实价格后逐项启用。知识星球邀请码按批次选择 `30`、`90` 或 `365` 天，默认 `30` 天，并通过独立领取入口发放，不得混入普通会员购买页。两种来源共用 VIP 能力，但期限、来源和审计必须分开记录。
- 未登录用户不能只凭邀请码生成匿名 VIP 或占用名额：统一从 `/vip/claim` 创建短期、不预占名额的领取凭证，经“派聪明”扫码绑定真实账号并同意当前协议后，才在一个事务内核销邀请码并开通 VIP。邀请码和领取令牌不得进入 URL、二维码或日志；一个账号终身只能领取一次邀请福利。首发扫码桥关闭期间，新用户扫码绑定领取暂不可用；已有邮箱账号仍可登录后领取。
- 阶段按 `free → membership-acceptance → membership → marketplace-acceptance → marketplace` 切换；验收模式只用于隔离环境，正式阶段允许临时停单维护，但不能绕过真实验收确认。
- 2026-07-24 首次生产制品已完成前端检查与生产构建、后端 362 项测试、空库迁移、生产数据库备份恢复演练和浏览器冒烟；人工精修真实 OSS、邮件附件和收费链路仍因总开关关闭而未做生产验收，不能把代码测试等同于这些外部链路已可开放。
- PaiResume 已以版本 `6a8c5345beac-20260724T025733Z` 部署到 `/home/www/pairesume`，生产地址为 `https://resume.paicoding.com`。`pai-resume.service`、每日 MySQL 备份 timer 和每 5 分钟 ready-check timer 均已启用；应用只监听 `127.0.0.1:8084`。公网 TCP 3306 已由腾讯云拒绝规则和多地外部超时复测确认关闭，本机 MySQL TLS 连接正常。
- 扫码桥、后台 AI 密钥配置、人工精修 RAM 最小权限、真实支付/退款和异机备份仍待后续验收；首发对应入口和全部收款开关保持关闭。

## 已确认的生产部署决策（2026-07-24）

- 生产根目录固定为 `/home/www/pairesume`，不再使用现有模板中的 `/opt/pai-resume`。Nginx、systemd、环境示例、预检、发布和回滚脚本必须整体同步，不能只改一处。
- 复用现有 MySQL 8 实例，新增 `pai_resume` 数据库和唯一的 `pai_resume@localhost` 账号；应用与 Flyway 共用，账号强制 TLS、最多 12 个连接，只拥有 `pai_resume.*` 权限。备份仍由 Linux root 走本地 socket，不再拆多套账号。
- 微信支付复用现有生产商户主体和底层商户凭据；PaiResume 必须使用独立回调 URL、订单前缀、业务开关、对账和退款审计。首次生产部署仍保持支付 Provider 与所有新单开关闭合。
- Redis 本次不重启、不迁移、不纳管；继续复用现有手工进程，PaiResume 使用独立逻辑库 DB 1 和键前缀 `pairesume:prod:`。systemd 纳管另开维护窗口。
- Redis 现场核查确认当前进程仍由 root 在登录 session 中手工启动，`redis.service` 虽 enabled 但 inactive，且现有 unit 的停止命令指向默认端口、无法正确管理 6388 实例；当前仅使用 RDB、AOF 关闭。不得直接执行 `systemctl start redis`，必须安排维护窗口，先完成认证 BGSAVE、备份校验和回滚准备，再把同一实例纳入 systemd。
- 管理后台 AI 服务商加密配置仍未实现；首发从权限 `0600` 的环境文件读取 DeepSeek 配置，后续再实现掩码、轮换、连接测试和审计。
- Nginx 复用现有进程、80/443 与 `paicoding.com` 通配符证书，新增 `resume.paicoding.com` 独立站点；DNS 已解析，静态根目录为 `/home/www/pairesume/current/dist`，API 仅反代到 `127.0.0.1:8084`。
- 通配符证书已安装到 Nginx 实际路径并补齐 acme.sh reload hook；`nginx -t` 和受控 reload 已通过，当前证书有效期至 2026-10-08，旧证书备份位于 `/var/backups/pai-resume/nginx-cert-before-20260724T014121Z`。
- 生产机不执行 npm 前端构建，也不安装全局 Node.js。发布入口参考 `/Users/itwanger/Documents/GitHub/paicoding/launch.sh` 的一条命令体验，但改为本地 Linux 构建环境完成测试、前端 `dist`、后端 JAR 和运行制品组装，再上传并由远端 systemd 激活、健康检查和失败回滚。
- 用户导出已改为浏览器端生成，人工精修由浏览器直传私有 OSS；生产后端不再启动 Node/tsx PDF worker，发布包不再携带 Node、前端源码或 `node_modules`。OSS AccessKey Secret 只保存在服务端环境变量中；前端只得到短期 POST Policy、受限表单字段及非秘密的 AccessKey ID，策略精确绑定 Bucket、随机对象键、大小、类型、SHA-256、私有 ACL、加密和单次写入。
- 首发 `RESUME_REVIEW_ENABLED=false`、`RESUME_REVIEW_OSS_ENABLED=false`。总开关关闭时生产预检和 Java 启动校验不要求人工精修收件箱、OSS 或 RAM；准备开启时私有桶、精确 CORS、两条生命周期和最小权限 RAM 四个确认位必须全部严格为 `true`。
- 用户已明确选择不新增 Linux 系统账号；首发 systemd 沿用 root，并用 `ProtectSystem=strict`、最小 `ReadWritePaths`、无 capabilities、`NoNewPrivileges`、`-Xms128m -Xmx512m`、`MemoryMax=1024M` 和 `MemorySwapMax=0` 收紧范围。
- 每日数据库备份、就绪探测和恢复演练已落地；仍必须补齐异机备份副本、资源/磁盘/数据库告警及真实告警送达。
- 上述决策、实际发布结果、未完成项和后续发布流程以 `deploy/production-deployment-decisions.md` 为准。首次发布没有修改或重启生产 Redis、MySQL、paicoding、PaiSmart 或 javabetter；Nginx 仅在首次站点和证书安装时做过通过 `nginx -t` 的平滑 reload，主 PID 未变化。首个版本尚无 `previous` 回滚目标，第二次成功发布后才具备一键切回上一版本的条件。

## 上线阻断项

1. 编辑器自动保存必须在切换简历、切换页面、组件卸载和关闭窗口前可靠落盘，不能因防抖延迟静默丢失最后一次修改。
2. 正式部署必须具备域名解析、HTTPS、反向代理、单页应用路由回退、配置与密钥管理、数据库备份恢复、发布回滚和基础告警。
3. 会员支付必须补齐后台订单查询、退款处理、退款记录、异常订单处置、真实客服入口和退款规则，不能只产生“需要退款”状态。
4. 会员支付与用户简历市场必须使用独立开关。用户市场开放前必须具备先审后发、举报、侵权投诉、申诉和下架能力。
5. 正式上线前必须补齐隐私政策、服务条款、注册确认、找回密码、账号注销、第三方人工智能处理说明，并禁止在前后端日志中记录简历原文、模型完整返回和其他敏感信息。
6. 开放收款前必须使用真实微信商户完成下单、回调、主动查询、超时关闭、迟到支付和退款全链路验收，并验证告警送达及数据库恢复。
7. 人工精修服务端只允许读取自己签发并记录的私有 OSS 对象键，不接受用户提供的 URL、本地路径或对象键；直传 PDF 必须校验大小、MIME、文件头、摘要元数据和固化后的对象标识。照片仍只允许经过 MIME、体积校验的栅格图片 Data URL。
8. 生产前端构建必须注入并在产物中核对真实运营主体、客服邮箱、AI 服务商名称和隐私政策链接；不得用占位值上线。
9. 正式宣称扫码登录可用前必须启用并真实验证派聪明服务号扫码桥；2026-07-24 首发允许以 `PAICONGMING_WECHAT_LOGIN_ENABLED=false` 启动邮箱登录灰度，且不得修改或重启 paicoding。首发同时保持 `RESUME_REVIEW_ENABLED=false`；开启人工精修前才强制验证私有 OSS、精确 CORS、短期签名、对象生命周期、最小权限 RAM 和邮件读取链路。人工精修不再要求第二个公众号或关注桥；第二次及以后收费前必须单独完成 `deploy/checklists/resume-review-acceptance.md`。

## 验收与执行边界

- 不能只根据页面、文档、配置或模拟测试宣称功能可上线，必须验证真实执行路径。
- 默认只在本地修改和验证，按改动风险选择检查范围。纯前端小改动只运行相关前端检查并做本地页面冒烟，不要求完整后端测试；涉及接口、数据库、后端逻辑或跨端契约时，再补充对应后端测试与完整构建。涉及数据库迁移时，必须在临时空库验证完整迁移。
- 需要让本地前端持续运行时，使用独立的 `tmux` 会话，并同时验证监听端口和 HTTP 响应。
- 只有用户明确要求“提交”时才提交，明确要求“推送”时才推送，明确要求“部署生产”或“更新到生产环境”时才部署；不得根据前序任务或连续验收过程推断本次也需要生产发布。未经明确要求，不改变正式支付开关；保留工作区中已有的未提交和未跟踪改动。
