# PaiResume 协作约定

## 当前产品基线

- 首页已确认基本完成，当前阶段默认冻结。除非用户明确要求，不再主动调整首页布局、文案和动效。
- 首页如需继续修改，只使用真实业务数据；不得虚构评分、浏览量、成交量或用户评价，优先展示免费/付费、价格等已有字段。
- 当前重点是上线安全与业务闭环，不是继续打磨展示层。上线范围按“免费或邀请灰度 → 会员支付 → 用户付费简历市场”逐步开放。
- 下述上线阻断项完成前，不正式开放收款；用户发布的付费简历市场保持关闭，首发只允许官方精选内容。
- 对外文案和入口必须读取服务端业务开关；市场关闭时不得宣传成当前可购买、可投稿。

## 当前实施状态

- 三阶段业务代码、搜索引擎优化、账户与隐私闭环、自动保存、安全边界和发布运维脚本均已落地；旧支付总开关、会员新订单和用户市场默认关闭。
- 登录主入口已改为“派聪明”服务号扫码注册/登录：PaiResume 不保存 AppSecret/access_token，只通过 paicoding 双向 HMAC 网关生成临时二维码并接收可信事件；纯扫码账号邮箱和密码为空，注销前必须重新扫码取得一次性凭证。邮箱登录只保留为兼容入口。
- “派聪明”只负责登录，不能作为人工精修第二次免费的关注凭证。人工精修固定为：第一次免费；第二次经独立“沉默王二”公众号验证后免费一次；第三次及以后每份单独付费。
- 人工精修只由服务端锁定不可变快照、渲染 PDF 并投递到固定私密收件箱；SMTP 受理后才核销免费次数，已发送副本不可召回。真实单价默认 `0`，独立新单开关默认关闭，未完成真实微信与邮件验收时必须拒绝付费新单。
- 付费 VIP 是年费会员，每笔默认 `365` 天并固化进订单快照；知识星球邀请码按批次选择 `30`、`90` 或 `365` 天，默认 `30` 天。两种来源共用 VIP 能力，但期限、来源和审计必须分开记录。
- 未登录用户不能只凭邀请码生成匿名 VIP 或占用名额：统一从 `/vip/claim` 创建短期、不预占名额的领取凭证，经“派聪明”扫码绑定真实账号并同意当前协议后，才在一个事务内核销邀请码并开通 VIP。邀请码和领取令牌不得进入 URL、二维码或日志；一个账号终身只能领取一次邀请福利。
- 阶段按 `free → membership-acceptance → membership → marketplace-acceptance → marketplace` 切换；验收模式只用于隔离环境，正式阶段允许临时停单维护，但不能绕过真实验收确认。
- 本地已通过后端全量测试、前端检查与生产构建、空库迁移、备份恢复、服务端 PDF 和浏览器搜索引擎优化冒烟；修改相关代码后仍须重新执行。
- 尚未执行生产部署。开放真实流量前仍需提供真实运营主体、客服邮箱和人工智能服务商信息，在目标 Linux 主机验证 Nginx 与 systemd，并完成真实微信商户支付、退款、告警送达及生产备份恢复验收。
- 新增扫码与人工精修仍缺真实外部配置/验收：派聪明双方桥接参数、独立“沉默王二”关注回调、人工精修固定收件箱、后台真实单价，以及真实扫码、邮件、支付和退款全链路证据。代码完成不等于已上线。

## 上线阻断项

1. 编辑器自动保存必须在切换简历、切换页面、组件卸载和关闭窗口前可靠落盘，不能因防抖延迟静默丢失最后一次修改。
2. 正式部署必须具备域名解析、HTTPS、反向代理、单页应用路由回退、配置与密钥管理、数据库备份恢复、发布回滚和基础告警。
3. 会员支付必须补齐后台订单查询、退款处理、退款记录、异常订单处置、真实客服入口和退款规则，不能只产生“需要退款”状态。
4. 会员支付与用户简历市场必须使用独立开关。用户市场开放前必须具备先审后发、举报、侵权投诉、申诉和下架能力。
5. 正式上线前必须补齐隐私政策、服务条款、注册确认、找回密码、账号注销、第三方人工智能处理说明，并禁止在前后端日志中记录简历原文、模型完整返回和其他敏感信息。
6. 开放收款前必须使用真实微信商户完成下单、回调、主动查询、超时关闭、迟到支付和退款全链路验收，并验证告警送达及数据库恢复。
7. 服务端 PDF、公开快照等链路不得主动读取用户提供的 URL 或本地路径；照片只允许经过 MIME、体积校验的栅格图片 Data URL。
8. 生产前端构建必须注入并在产物中核对真实运营主体、客服邮箱、AI 服务商名称和隐私政策链接；不得用占位值上线。
9. 生产预检必须启用派聪明服务号扫码桥和独立“沉默王二”公众号关注桥；两套身份、回调和密钥严禁混用。人工精修收费前必须单独完成 `deploy/checklists/resume-review-acceptance.md`。

## 验收与执行边界

- 不能只根据页面、文档、配置或模拟测试宣称功能可上线，必须验证真实执行路径。
- 代码改动后至少运行后端完整测试、前端构建、前端检查和 `git diff --check`；涉及数据库迁移时，必须在临时空库验证完整迁移。
- 需要让本地前端持续运行时，使用独立的 `tmux` 会话，并同时验证监听端口和 HTTP 响应。
- 未经用户明确要求，不提交、不推送、不部署生产环境、不改变正式支付开关；保留工作区中已有的未提交和未跟踪改动。
