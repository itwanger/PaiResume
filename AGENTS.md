# PaiResume 协作约定

## 当前产品基线

- 当前首发目标是知识星球会员自助使用：派聪明扫码注册/登录、知识星球邀请码开通 VIP、简历编辑保存、排版导出和 AI 分析属于同一组生产必备能力。部署后按 `deploy/checklists/planet-core-acceptance.md` 完成真实功能验收并记录结果，不能用尚未完成的验收标记阻止部署待验收版本；人工精修、会员购买和用户市场分别验收，不能用页面存在或二维码生成代替真实结果。
- 首页已确认基本完成，当前阶段默认冻结。除非用户明确要求，不再主动调整首页布局、文案和动效。
- 首页如需继续修改，只使用真实业务数据；不得虚构评分、浏览量、成交量或用户评价，优先展示免费/付费、价格等已有字段。
- 当前重点是上线安全与业务闭环，不是继续打磨展示层。所有购买统一使用后台微信支付配置；用户市场内容开放单独验收。
- 下述适用的验收要求完成前，不正式开放收款；用户发布的付费简历市场保持关闭，首发只允许官方精选内容。
- 受业务开关控制的功能，其对外文案和入口必须与服务端状态一致；市场关闭时不得宣传成当前可购买、可投稿。人工精修按下述常驻服务规则执行。

## 用户界面文案规则

- 默认不添加常驻的解释性副标题、帮助文案或实现细节。控件名称、布局和状态已经能表达含义时，不再用文字重复解释。
- 禁止使用“已按……推算，可修改”“只影响……”“不会覆盖……”“点击后……”等描述显而易见行为的文案。若必须依靠说明才能理解，应先改进名称、结构、交互或状态反馈。
- 仅保留会影响用户决策或操作结果的信息：输入与上传限制、可执行的错误恢复、保存和处理状态、付费与退款结果、不可逆操作后果，以及隐私、安全、授权和法律提示。
- 新增辅助文案前判断“删掉后是否会导致用户做错、无法恢复或承担未知风险”；答案为否时不添加。修改时检索同一复用组件及直接调用处，确认同源问题后同步修复；独立页面的问题记录为发现，不自动扩大修改范围。

## 用户界面容器规则

- 同一块内容默认只保留一层承载容器，不为制造层次连续叠加圆角、边框、背景和阴影；禁止无语义的“卡片套卡片”。
- 优先用留白、字号字重、浅色背景和单向分隔线表达层级。输入控件、弹窗、错误提示、可独立操作的重复条目和拖拽项可以保留明确边界。
- 发现冗余套框时，检查对应复用组件及直接调用处，确认同源问题后同步修复；验证只覆盖实际改动及直接影响。

## 现行业务约束

- 支付状态仅由后台微信支付配置决定，配置完整并启用后所有购买共同可用，不设置模块新单开关。
- 登录目标主入口已改为“派聪明”服务号扫码注册/登录：PaiResume 不保存 AppSecret/access_token，只通过 paicoding 双向 HMAC 网关生成临时二维码并接收可信事件；纯扫码账号邮箱和密码为空，注销前必须重新扫码取得一次性凭证。保留邮箱兼容登录。
- “派聪明”只负责登录。人工精修是 VIP 会员权益：有效会员可以免费排队，非会员必须先开通会员；会员可自愿支付加急金额，金额越高等待位置越靠前。产品不展示第二个二维码，也不再接入独立关注桥。
- 普通 PDF 导出只在浏览器客户端完成。申请人工精修时，用户直接选择已导出的本地 PDF；服务端在内存中复核大小、MIME、`%PDF-` 文件头和 SHA-256 后，立即作为附件投递到后台配置的私密收件箱，不写服务器本地磁盘、不使用 OSS。SMTP 接受邮件后申请才进入公开队列，已发送副本不可召回。人工精修是常驻服务，不设置业务开关；导航与排版导出入口始终存在。免费会员按排队时间等待；自选加急金额越高位置越靠前，同金额按付款时间排列；已开始精修的申请不被后来申请替换。
- 付费 VIP 提供月卡 `30` 天、季卡 `90` 天、年卡 `365` 天和终身会员，方案编码、权益类型、期限与价格由服务端 `membership_plan` 管理并固化进订单快照；迁移时只继承并启用已有年卡价格，其余方案必须由管理员配置真实价格后逐项启用。知识星球邀请码按批次选择 `30`、`90` 或 `365` 天，默认 `30` 天，并通过独立领取入口发放，不得混入普通会员购买页。两种来源共用 VIP 能力，但期限、来源和审计必须分开记录。
- 未登录用户不能只凭邀请码生成匿名 VIP 或占用名额：统一从 `/vip/claim` 创建短期、不预占名额的领取凭证，经“派聪明”扫码绑定真实账号并同意当前协议后，才在一个事务内核销邀请码并开通 VIP。邀请分享链接允许携带邀请码（如 `/?VIP...`），进入登录页后清理地址栏并先校验邀请码，再生成关联的登录二维码。领取令牌和轮询凭证不得进入 URL、二维码或日志，业务日志不得记录邀请码；一个账号终身只能领取一次邀请福利。
- 支付不再通过部署阶段或验收确认环境变量开关；真实商户下单、回调、退款等仍需留存验证结果。
## 部署与共享资源边界

- 生产根目录固定为 `/home/www/pairesume`；修改部署路径时同步相关 Nginx、systemd、环境示例、预检、发布和回滚配置。应用仅监听本机回环地址。
- MySQL 使用独立的 `pai_resume` 数据库及最小权限账号，强制 TLS；Redis 使用 DB 1 和 `pairesume:prod:` 前缀。不得擅自重启、迁移或纳管共享 MySQL、Redis、Nginx 及其他项目服务。
- Redis 纳管须单独授权并安排维护窗口，先完成备份校验与回滚准备；不得直接执行 `systemctl start redis`。数据库恢复须单独确认，不能随代码回滚自动覆盖生产数据。
- 不新增 Linux 系统账号；保留现有 systemd 的只读文件系统、最小可写路径、无 capabilities、`NoNewPrivileges` 和内存限制。详细配置仅在修改部署或运行权限时查阅部署文档。
- 人工精修的生产预检和 Java 启动仍要求真实私密收件箱、SMTP 授权码和公开 Message-ID 域名；不要求人工精修 OSS。
- 发布使用本项目 `launch.sh`，在本地受控 Linux 环境生成制品；生产机不执行 npm/Maven 构建，不安装或随包携带 Node，不启动 PDF worker。按本次改动完成定向验证，发布不隐式追加全量测试。
- 部署任务按需查阅 `deploy/production-deployment-decisions.md` 的对应章节；核心功能验收使用 `deploy/checklists/planet-core-acceptance.md`，人工精修验收使用 `deploy/checklists/resume-review-acceptance.md`。普通代码修改不要求通读部署或验收文档。
- 历史版本、测试数量、证书有效期和现场服务状态留在部署记录中，不代表当前状态或新的执行授权；现行规则优先于历史快照，涉及实际操作时核对相关当前状态。

## 部署与功能开放的验收要求

1. 编辑器自动保存必须在切换简历、切换页面、组件卸载和关闭窗口前可靠落盘，不能因防抖延迟静默丢失最后一次修改。
2. 正式部署必须具备域名解析、HTTPS、反向代理、单页应用路由回退、配置与密钥管理、数据库备份恢复、发布回滚和基础告警。
3. 会员支付必须补齐后台订单查询、退款处理、退款记录、异常订单处置、真实客服入口和退款规则，不能只产生“需要退款”状态。
4. 会员、官方精选简历、人工精修加急与已开放市场的付费简历必须共用后台微信支付状态，不新增模块支付开关。用户市场开放前必须具备先审后发、举报、侵权投诉、申诉和下架能力。
5. 正式上线前必须补齐隐私政策、服务条款、注册确认、找回密码、账号注销、第三方人工智能处理说明，并禁止在前后端日志中记录简历原文、模型完整返回和其他敏感信息。
6. 开放收款前必须使用真实微信商户完成下单、回调、主动查询、超时关闭、迟到支付和退款的完整业务验收，并验证告警送达及数据库恢复。
7. 人工精修提交只接受 `multipart/form-data` 中的 PDF，必须复核文件名、大小、MIME、`%PDF-` 文件头和创建申请时记录的 SHA-256；邮件接受前不得进入公开队列。照片的私有 OSS 规则保持不变。
8. 生产前端构建必须注入并在产物中核对真实运营主体、客服邮箱、AI 服务商名称和隐私政策链接；不得用占位值上线。
9. 下一次生产发布必须保持 `PAICONGMING_WECHAT_LOGIN_ENABLED=true`，并真实验证派聪明服务号扫码回调、注册落库、知识星球邀请码核销、编辑保存、排版导出和 AI 分析。人工精修不再要求第二个公众号或关注桥；开放会员免费排队前另完成 `deploy/checklists/resume-review-acceptance.md`，开放加急前再完成其中的真实支付验收。

## 验收与执行边界

- 用户已要求实现或修复时，可连续完成本地修改、最小必要验证和本次引入问题的修复；已有授权范围内无需逐步确认。只在缺失信息会实质影响结果且无法合理判断，或操作超出授权范围时询问。
- 不能只根据页面、文档、配置或模拟测试宣称功能可上线，必须验证真实执行路径。
- 默认只在本地修改和验证，验证范围以本次实际改动为准。页面、布局、文案和交互修改，只检查涉及的页面及直接关联行为；不要顺带运行后端测试、其他页面测试或完整构建。
- 涉及后端逻辑、接口或跨端契约时，只验证修改的方法、接口或具体场景。必须优先使用测试方法或用例名称筛选；禁止为了方便按整个测试类、模块、目录批量运行，把未改动场景带进来。不要因为修改了支付、权限等模块，就自动扩大到该模块全部测试。
- 验证前先确定最小必要检查。相关检查通过后即停止，不再追加“保险起见”的回归、重复测试、完整类型检查或生产构建。只有出现具体失败，或修改引入了明确的额外影响，才扩展到对应场景，并说明原因。
- 数据库迁移只在临时空库验证本次相关迁移。完整前后端测试套件或全面回归，只有用户在当前任务中明确要求“全量测试”“完整测试”或“全面跑一遍”时才执行；不得从此前要求、模块重要性或改动规模推断授权。
- 需要让本地前端持续运行时，使用独立的 `tmux` 会话，并同时验证监听端口和 HTTP 响应。
- 只有用户明确要求“提交”时才提交，明确要求“推送”时才推送，明确要求“部署生产”或“更新到生产环境”时才部署；不得根据前序任务或连续验收过程推断本次也需要生产发布。未经明确要求，不改变正式支付开关；保留工作区中已有的未提交和未跟踪改动。
