# TODO

- [ ] 将 PaiResume 的 Flyway 数据库迁移治理与上线前测试流程整理成简历项目亮点。
  - Flyway：兼容历史数据库与全新空库，保留 V1-V5 历史迁移，通过 V5 baseline、V6 全量结构校准、V7 邀请码、V8 会员来源追踪/审计/异常撤销、V9 简历市场、V10 支付生命周期加固、V11 收益冻结与退款反冲、V12 跨节点支付对账租约完成平滑升级；修复旧迁移中写死库名和版本间字段不一致导致的空库部署失败，并通过独立迁移账号、启动前配置校验降低生产误操作风险。
  - 自动化测试：后端执行带 Byte Buddy agent 的 `mvn test`，当前 166 项测试全部通过，覆盖认证、JWT、验证码、登录/邀请码限流、会员、邀请码、邮件、AI/PDF 权限、迁移策略、市场支付、收益结算与退款反冲等；前端执行 `npm run build`、`npm run lint`，最后执行 `git diff --check`。
  - 数据库验收：验证含 24 个用户、53 份简历的 V8 历史库副本连续迁移到 V9、V10 且业务数据数量不变；本次另用真实 MySQL 8 临时空库从 V5 baseline 连续迁移到 V12，并通过真实交易链路覆盖 V11 账本字段、V12 租约索引和 MySQL 专用对账 SQL。
  - 真实流程验收：除会员、邀请码与编辑导出链路外，使用浏览器和真实 API 完成用户简历免费/付费公开、模拟 Native 支付、一次购买持续查看、不可变快照、收益冻结与释放、作者申请线下结算、管理员登记转账、全额退款撤权与反冲、已打款退款形成欠款、再次购买优先抵债、审核暂停历史买家访问、免费公开访问，以及官方精选 VIP/非 VIP 路由验证。
  - 简历表述草稿：设计并落地基于 Flyway 的版本化数据库迁移体系，兼容历史库平滑升级和全新环境初始化；建立“单元测试 + 生产构建 + 历史库/空库迁移 + 基础设施就绪检查 + 浏览器/API E2E”的上线前质量门禁，累计通过 166 项后端测试，并在真实链路验收中发现和修复迁移、会员状态持久化、订单唯一键释放、退款再购买、权限边界、限流与 HTTP 语义问题。
