# TODO

- [ ] 将 PaiResume 的 Flyway 数据库迁移治理与上线前测试流程整理成简历项目亮点。
  - Flyway：兼容历史数据库与全新空库，保留 V1-V5 历史迁移，通过 V5 baseline、V6 全量结构校准、V7 邀请码、V8 会员来源追踪/审计/异常撤销、V9 简历市场、V10 支付生命周期加固、V11 收益冻结与退款反冲、V12 跨节点市场支付对账租约、V13 会员在线支付订单、V14 市场浏览量、V15 会员退款复核、V16 市场治理、V18 账号隐私与注销字段完成平滑升级；修复旧迁移中写死库名和版本间字段不一致导致的空库部署失败，并通过独立迁移账号、启动前配置校验降低生产误操作风险。
  - 自动化测试：后端执行 `mvn test`，截至 2026-08-19 为 538 项通过、1 项 opt-in 真实迁移测试跳过（配置隔离 MySQL 后单独验证通过），覆盖认证、JWT、验证码、协议确认、密码重置、账号注销、登录/邀请码限流、会员、邀请码、邮件、AI/PDF 权限、迁移策略、市场支付、会员支付、退款复核、市场治理、收益结算、退款反冲、照片安全、反馈限流脱敏、浏览量去重、分场景简历分析（服务/接口/SSE 契约）和 AI 服务商加密配置（加密、掩码、权限、脱敏）等；前端执行 Vitest 组件测试 117 项、node --import tsx 单元测试 99 项、Playwright 浏览器 E2E 6 项（登录态、注销前置、自动保存恢复、管理深链），以及 `npm run build`、`npm run lint`、`git diff --check`。
  - 数据库验收：验证含 24 个用户、53 份简历的 V8 历史库副本连续迁移到 V9、V10 且业务数据数量不变；2026-08 继续使用真实 MySQL 8 临时空库从 V5 baseline 连续迁移到 V31，检查 V11 账本字段、V12 市场支付租约索引、V13/V15 会员订单与退款复核、V16 市场治理表、V18 账号隐私字段、V30 分求职场景分析提示词表（四条种子与场景索引、二次迁移不破坏种子、历史订单保持不变）和 V31 AI 服务商加密配置表（单行约束与审计表、种子关闭状态）。
  - 2026-08 新增能力：按工作党/日常实习/暑期实习/秋招四种求职场景的 AI 简历分析（后台维护场景 Prompt，请求不可绕过）；管理后台 AI 服务商安全配置（API Key AES-256-GCM 加密落库、主密钥仅存 0600 环境文件、查询仅掩码、留空保留/显式轮换、连接测试、变更审计、服务商切换联动用户重新披露同意）；SSE 流式分析在流前失败（非 VIP、越权、非法场景）时以真实错误事件替代空响应体；Playwright 浏览器 E2E 基线。
  - 真实流程验收：使用浏览器和真实 API 完成用户简历免费/付费公开、模拟 Native 支付、一次购买持续查看、不可变快照、收益冻结与释放、作者申请线下结算、管理员登记转账、全额退款撤权与反冲、已打款退款形成欠款、再次购买优先抵债、审核暂停历史买家访问、免费公开访问，以及官方精选 VIP/非 VIP 路由验证；会员支付补充覆盖订单金额与 30 天权益快照、30 分钟倒计时、回调/主动查单原子开通、有限期续期、0 元优惠码本地结算、超时先查后关和迟到付款人工退款边界。
  - 简历表述草稿：设计并落地基于 Flyway 的版本化数据库迁移体系，兼容历史库平滑升级和全新环境初始化；建立“单元测试 + 生产构建 + 历史库/空库迁移 + 基础设施就绪检查 + 浏览器/API E2E”的上线前质量门禁，截至 2026-08-19 累计通过 538 项后端测试与 222 项前端测试（组件 117 + 单元 99 + E2E 6），并在真实链路验收中发现和修复迁移、会员状态持久化、订单唯一键释放、退款再购买、支付超时竞态、权限边界、限流、HTTP 语义、SSE 错误契约和密钥加密治理问题；生产部署快照仍为 2026-07-24 的 V23 首发版本（`6a8c5345beac-20260724T025733Z`），本地 V24-V31 尚未发布，支付、人工精修与用户市场仍按清单处于验收前关闭状态。
